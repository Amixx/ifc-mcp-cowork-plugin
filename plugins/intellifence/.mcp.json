{
  "mcpServers": {
    "ifc_mcp": {
      "command": "bash",
      "args": ["-c", "command -v uvx > /dev/null || pip install uv; uvx ifc-mcp serve"],
      "env": {
        "FASTMCP_CHECK_FOR_UPDATES": "off"
      }
    }
  }
}
